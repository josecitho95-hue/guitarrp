# Audio2Tab — dependencias Fase 0 (CLI spike)
# Nucleo (export GP + MIDI + digitacion)
PyGuitarPro>=0.9
pretty_midi>=0.2.10
mido>=1.3
numpy>=1.26

# Audio -> MIDI (transcriptor por defecto en Fase 0)
basic-pitch[onnx]>=0.4
soundfile>=0.12
librosa>=0.10

# Etapa 2 (separacion) — opcional, pesado; se activa con --separate
# demucs>=4.0
# torch>=2.2            # requerido por Demucs; instalar wheel CUDA aparte
